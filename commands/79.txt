SELECT student_id, student_name, SUM(marks) AS total_marks
FROM students_marks
WHERE is_absent = 0 AND is_passed = 1 AND marks >= 60
GROUP BY student_id
HAVING COUNT(*) = (SELECT COUNT(DISTINCT subject_name) FROM students_marks)
ORDER BY total_marks DESC
LIMIT 3;